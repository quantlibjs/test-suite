describe('Market-model tests', () => {
    it('Testing exact repricing of one-step forwards and optionlets in a' +
        ' lognormal forward rate market model...', () => {
    });
    it('Testing exact repricing of one-step forwards and optionlets' +
        ' in a normal forward rate market model...', () => {
    });
    it('Testing exact repricing of inverse floater in forward' +
        ' rate market model...', () => {
    });
});